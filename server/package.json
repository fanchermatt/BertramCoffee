{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "bootstrap-server": "(npm install) && (npm run createSchema) && (npm run migrations)  && (npm run seeds) && (node server.js)",
    "createSchema": "npx sequelize db:create",
    "migrations": "npx sequelize db:migrate",
    "seeds": "npx sequelize db:seed:all",
    "dropSchema": "npx sequelize db:drop"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-validator": "^7.2.1",
    "fs": "^0.0.1-security",
    "mysql2": "^3.14.1",
    "path": "^0.12.7",
    "sequelize": "^6.37.7",
    "sequelize-cli": "^6.6.3",
    "uuid": "^11.1.0"
  }
}
